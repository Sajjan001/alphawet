# Cloudflare Workers static-assets config (deploy command: `npx wrangler
# deploy --assets dist`). No Pages Functions in this project anymore — this
# is a pure static-asset deployment.

name = "alphawetai"
compatibility_date = "2026-08-15"

[assets]
directory = "./dist"
